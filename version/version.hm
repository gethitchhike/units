<?php
$this->version = function(){
        if ($this->args[1] == "help"){
                return "Get version\n";
        }
        $v = new Version();
        $version = $v->GetVersion();
        echo $version."\n";
};
$this->dynamicMethods[]= "version";

